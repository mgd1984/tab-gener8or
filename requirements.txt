# Core dependencies
streamlit>=1.24.0,<2.0.0
basic-pitch==0.4.0           --no-deps   # install it but ignore its old pins
tayuya==0.0.4

# Audio processing
numpy>=1.26,<2.0
librosa>=0.10.0,<0.11.0
matplotlib>=3.7.0,<3.8.0

# MIDI handling
mido>=1.2.10,<2.0.0

# Required for basic-pitch
scikit-learn==1.4.2
tensorflow-cpu==2.16.2                    # first cp310 / cp311 wheel with proto 3.20+

# Build tools (try adding specific versions for compatibility)
setuptools==68.0.0
wheel==0.41.2

--only-binary scikit-learn